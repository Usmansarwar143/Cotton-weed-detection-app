# Auto detect text files and perform LF normalization
* text=auto

# TFLite model — tracked by Git LFS
*.tflite filter=lfs diff=lfs merge=lfs -text

# Binary assets — no line ending conversion
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.ttf binary
*.woff binary
*.woff2 binary
